---
- name: Install Grafana Alloy
  hosts: masters
  become: true
  gather_facts: false

  vars_files:
    - vars/cluster.yml

  tasks:
    - name: Check Kubernetes administrator kubeconfig
      ansible.builtin.stat:
        path: "{{ kubeconfig_path }}"
      register: alloy_kubeconfig

    - name: Fail when Kubernetes control plane is not initialized
      ansible.builtin.assert:
        that:
          - alloy_kubeconfig.stat.exists
        fail_msg: >-
          Kubernetes administrator kubeconfig was not found at
          {{ kubeconfig_path }}.
        success_msg: Kubernetes administrator kubeconfig was found.

    - name: Check Helm installation
      ansible.builtin.command:
        argv:
          - helm
          - version
          - --short
      register: alloy_helm_version
      changed_when: false

    - name: Show Helm version
      ansible.builtin.debug:
        msg: "{{ alloy_helm_version.stdout }}"

    - name: Check monitoring namespace
      ansible.builtin.command:
        argv:
          - kubectl
          - get
          - namespace
          - "{{ alloy_namespace }}"
      environment:
        KUBECONFIG: "{{ kubeconfig_path }}"
      register: alloy_namespace_status
      changed_when: false
      failed_when: false

    - name: Create monitoring namespace
      ansible.builtin.command:
        argv:
          - kubectl
          - create
          - namespace
          - "{{ alloy_namespace }}"
      environment:
        KUBECONFIG: "{{ kubeconfig_path }}"
      when: alloy_namespace_status.rc != 0

    - name: Add Grafana Helm repository
      ansible.builtin.command:
        argv:
          - helm
          - repo
          - add
          - "{{ alloy_repository_name }}"
          - "{{ alloy_repository_url }}"
          - --force-update
      register: alloy_helm_repository
      changed_when: >-
        'has been added' in alloy_helm_repository.stdout

    - name: Update Helm repositories
      ansible.builtin.command:
        argv:
          - helm
          - repo
          - update
      changed_when: false

    - name: Validate Alloy chart version
      ansible.builtin.assert:
        that:
          - alloy_chart_version is defined
          - alloy_chart_version | length > 0
        fail_msg: >-
          Grafana Alloy chart version is empty.
          Configure alloy_chart_version in vars/cluster.yml.

    - name: Verify Alloy chart version
      ansible.builtin.command:
        argv:
          - helm
          - show
          - chart
          - "{{ alloy_chart }}"
          - --version
          - "{{ alloy_chart_version }}"
      register: alloy_chart_information
      changed_when: false

    - name: Show Alloy chart information
      ansible.builtin.debug:
        msg: "{{ alloy_chart_information.stdout_lines }}"

    - name: Render Alloy Helm values
      ansible.builtin.template:
        src: templates/alloy-values.yml.j2
        dest: "{{ alloy_values_remote_path }}"
        owner: root
        group: root
        mode: "0600"

    - name: Validate Alloy Helm template
      ansible.builtin.command:
        argv:
          - helm
          - template
          - "{{ alloy_release_name }}"
          - "{{ alloy_chart }}"
          - --version
          - "{{ alloy_chart_version }}"
          - --namespace
          - "{{ alloy_namespace }}"
          - --values
          - "{{ alloy_values_remote_path }}"
      changed_when: false

    - name: Install or upgrade Grafana Alloy
      ansible.builtin.command:
        argv:
          - helm
          - upgrade
          - --install
          - "{{ alloy_release_name }}"
          - "{{ alloy_chart }}"
          - --version
          - "{{ alloy_chart_version }}"
          - --namespace
          - "{{ alloy_namespace }}"
          - --values
          - "{{ alloy_values_remote_path }}"
          - --wait
          - --atomic
          - --timeout
          - 10m
          - --history-max
          - "10"
      environment:
        KUBECONFIG: "{{ kubeconfig_path }}"
      register: alloy_helm_install

    - name: Show Helm installation result
      ansible.builtin.debug:
        msg: "{{ alloy_helm_install.stdout_lines }}"

    - name: Wait for Alloy DaemonSet rollout
      ansible.builtin.command:
        argv:
          - kubectl
          - rollout
          - status
          - "daemonset/{{ alloy_release_name }}"
          - --namespace
          - "{{ alloy_namespace }}"
          - --timeout=300s
      environment:
        KUBECONFIG: "{{ kubeconfig_path }}"
      changed_when: false

    - name: Get Alloy DaemonSet
      ansible.builtin.command:
        argv:
          - kubectl
          - get
          - daemonset
          - --namespace
          - "{{ alloy_namespace }}"
          - -l
          - "app.kubernetes.io/instance={{ alloy_release_name }}"
          - -o
          - wide
      environment:
        KUBECONFIG: "{{ kubeconfig_path }}"
      register: alloy_daemonset
      changed_when: false

    - name: Get Alloy pods
      ansible.builtin.command:
        argv:
          - kubectl
          - get
          - pods
          - --namespace
          - "{{ alloy_namespace }}"
          - -l
          - "app.kubernetes.io/instance={{ alloy_release_name }}"
          - -o
          - wide
      environment:
        KUBECONFIG: "{{ kubeconfig_path }}"
      register: alloy_pods
      changed_when: false

    - name: Get Alloy services
      ansible.builtin.command:
        argv:
          - kubectl
          - get
          - services
          - --namespace
          - "{{ alloy_namespace }}"
          - -l
          - "app.kubernetes.io/instance={{ alloy_release_name }}"
      environment:
        KUBECONFIG: "{{ kubeconfig_path }}"
      register: alloy_services
      changed_when: false

    - name: Get Helm release status
      ansible.builtin.command:
        argv:
          - helm
          - status
          - "{{ alloy_release_name }}"
          - --namespace
          - "{{ alloy_namespace }}"
      environment:
        KUBECONFIG: "{{ kubeconfig_path }}"
      register: alloy_helm_status
      changed_when: false

    - name: Show Alloy DaemonSet
      ansible.builtin.debug:
        msg: "{{ alloy_daemonset.stdout_lines }}"

    - name: Show Alloy pods
      ansible.builtin.debug:
        msg: "{{ alloy_pods.stdout_lines }}"

    - name: Show Alloy services
      ansible.builtin.debug:
        msg: "{{ alloy_services.stdout_lines }}"

    - name: Show Helm release status
      ansible.builtin.debug:
        msg: "{{ alloy_helm_status.stdout_lines }}"